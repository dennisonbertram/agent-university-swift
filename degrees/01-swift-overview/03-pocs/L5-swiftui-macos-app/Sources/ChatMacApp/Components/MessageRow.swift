// MessageRow.swift — single chat bubble (STUB)

import SwiftUI
import ChatAppCore
import AnthropicClient

struct MessageRow: View {
    let message: ChatMessage

    var body: some View {
        Text("TODO")
    }
}
