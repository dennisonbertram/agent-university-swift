// ContentView.swift — main chat screen (STUB)

import SwiftUI
import ChatAppCore

struct ContentView: View {
    @Bindable var vm: ChatViewModel

    var body: some View {
        Text("TODO")
    }
}
